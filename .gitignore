# macOS Finder metadata. Written invisibly every time a folder is opened,
# which is why it kept appearing in GitHub Desktop.
.DS_Store
._*
.AppleDouble
.Spotlight-V100
.Trashes

# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
.bundle/
vendor/
